# Homebrew Tap

Custom Homebrew tap for Diktator — local voice-to-text for Mac.

## Install

```bash
brew tap sawasawasawa/tap
brew install --cask diktator
```

## Update

```bash
brew upgrade --cask diktator
```

## Uninstall

```bash
brew uninstall --cask diktator
```
